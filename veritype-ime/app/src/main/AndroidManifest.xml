<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Deliberately NO <uses-permission> declarations:
          - NO android.permission.INTERNET      (nothing ever leaves the device)
          - NO accessibility permissions         (no AccessibilityService of any kind)
          - NO storage permissions               (export uses the Storage Access Framework)

        The only privileged binding in this app is BIND_INPUT_METHOD on the service
        below — the standard, user-visible permission every soft keyboard requires.
        Android itself shows the user a warning when they enable ANY third-party IME,
        which is part of the consent-bounded design.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.VeriType"
        android:supportsRtl="true">

        <service
            android:name=".VeriTypeIMEService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
